There is a script called xfusioncorp.sh located in /tmp on App Server 2.
Right now, the script cannot be run because it does not have the correct permissions.

The task is to:
Give executable permission to the script.
Make sure all users (not just the owner) can execute it.
